using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Net.Cache;
using System.Xml.Linq;
class Program
{
    static void StoreGetEmployeeInfo(string Name , double Salary)
    {
        List<(string, double)> list = new List<(string, double)>{
         
        };

        list.Add((Name, Salary));
        Console.WriteLine($"{Name} info Added successfully");
    }
    static void Main()
    {

        //Problem: Use tuples to store employee names and salaries, and compare them.
        var employee1 = (Name: "Alice", Salary: 50000);
        var employee2 = (Name: "Bob", Salary: 60000);

        Console.WriteLine($" {employee1.Name} has {(employee1.Salary > employee2.Salary ? "higher" : "lower")}  salary than {employee2.Name} ");


        StoreGetEmployeeInfo("Said", 4000);


        // Output: Name: Player1, Health: 100, Score: 2000

        Console.ReadKey();
    }
}
