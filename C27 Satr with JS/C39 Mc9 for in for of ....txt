<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>JavaScript Type System Demo</title>

    <style></style>
  </head>
  <body>
    <script>
            
      const ages = [12, 17, 19];

      const hasAdult = ages.some(a => a >= 18);

      console.log(hasAdult);


      const values = [40, 5, 90, 12];
      values.sort( (a,b) => a-b )
      console.log(values);

      const cities = ["Amman", "Zarqa", "Irbid"];
      cities.sort( (a,b) => a-b )
      console.log(cities);


      const data = [3, 7, -2, 10];
      console.log( data.some( (x) => x<0 ));


      const list = [2, 4, 6, 8];
      console.log( list.every( (x) => x % 2 === 0 ) );

      /*1️⃣ Loop over object properties

        2️⃣ Stop looping when value is found

        3️⃣ Convert prices to tax-included prices

        4️⃣ Log each DOM element
      */

      
    </script>
  </body>
</html>
