import 'dart:async';
import 'dart:math'; // import built in package
import 'core/functions.dart'; // importing folders
import 'package:http/http.dart';

void main() async {
  //bool data = await getData();

  print("Start");
  // print(data? "Is True data" : "Is false");
  await getData().then(//then about the returned data from getData
    (val) => {val ? print("Data its true") : print("False data")},
  );
  print("End");
}

Future<bool> getData() {
  return Future.delayed(Duration(seconds: 3), () {
    return false;
  });
}
