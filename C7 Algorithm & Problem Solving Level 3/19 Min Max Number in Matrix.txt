#include <iostream>
#include <string>
#include <iomanip>

using namespace std;



void PrintMatrix1(int arr[3][3], short Rows, short Cols)
{
    for (int i = 0; i < Rows; i++)
    {
        for (int j = 0; j < Cols; j++)
        {
            cout << arr[i][j] << "\t";
            //cout << setw(3) << arr[i][j] << "\t";
        }
        cout << endl;
    }

}


int MinNumberInMatrix(int Matrix1[3][3], short Rows, short Cols)
{
    short Min = Matrix1[0][0];
    for (int i = 0; i < Rows; i++)
    {
        for (int j = 0; j < Cols; j++)
        {

            if (Matrix1[i][j] < Min)
            {
                Min = Matrix1[i][j];

            }

        }

    }

    return Min;

}


int MaxNumberInMatrix(int Matrix1[3][3], short Rows, short Cols)
{
    short Max = Matrix1[0][0];
    for (int i = 0; i < Rows; i++)
    {
        for (int j = 0; j < Cols; j++)
        {
            if (Matrix1[i][j] > Max)
            {
                Max = Matrix1[i][j];
               
            }

        }

    }

    return Max;

}



    
    






int main()
{
    srand((unsigned)time(NULL));

    int Matrix1[3][3] =
    { {0,99,5},
      {0,4,1},
      {5,-3,0}
    };
    short NumberToLook=0;

    cout << "Matrix1 :" << endl;
    PrintMatrix1(Matrix1, 3, 3);

    cout << "The min number is: " << MinNumberInMatrix(Matrix1, 3, 3) << endl;

    cout<< "The max number is: " <<MaxNumberInMatrix( Matrix1, 3, 3);


    


    return 0;
}
