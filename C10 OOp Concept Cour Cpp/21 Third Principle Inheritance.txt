// With two problems
//the first: Prametized constructor of the base class
//Adjust the PrintInfo function

#include <iostream>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <iomanip>

using namespace std;


class clsPerson
{
private:
    int _ID = 10;
    string _FirstName;
    string _LastName;
    string _FullName;
    string _Email;
    string _Phone;
    


public:

    /*clsEmployee(int I, string Fi, string L, string Fu, string E, string P) : _ID(I), _FirstName(Fi), _LastName(L), _FullName(Fu), _Email(E), _Phone(P)
    {
        cout << "I am constructor" << endl;
    }*/

    clsPerson()
    {

    }

    clsPerson(int ID, string FirstName, string LastName, string FullName,string Title, string Email, string Phone , int Salary, string Department)
    {
        _ID = ID;
        _FirstName = FirstName;
        _LastName = LastName;
        _FullName = FullName;
        _Email = Email;
        _Phone = Phone;
        cout << "I am constructor" << endl;
    }

    int GetID() // Read Only
    {
        return _ID;
    }
    void SetFirstName(string FirstName)
    {
        _FirstName = FirstName;
    }

    string GetFirstName()
    {
        return _FirstName;
    }

    void SetLastName(string LastName)
    {
        _LastName = LastName;
    }

    string GetLastName()
    {
        return _LastName;
    }

    /*void SetFullName(string FullName)
    {
        _FullName = FullName;
    }

    string GetFullName()
    {
        return _FullName;
    }*/

    string GetFullName()
    {
        return _FirstName + "" + _LastName;
    }

  
    void SetEmail(string Email)
    {
        _Email = Email;
    }

    string GetEmail()
    {
        return _Email;
    }

    void SetPhone(string Phone)
    {
        _Phone = Phone;
    }

    string GetPhone()
    {
        return _Phone;
    }

   
    void SendEmail(string Subject, string Body)
    {
        cout << "The following message sent successfully to email: " << _Email /*GetEmail()*/ << endl;
        cout << "Subject: " << Subject << endl;
        cout << "Body   : " << Body << endl;
    }

    void SendSMS(string TextMessage)
    {
        cout << "The following message sent successfully to Phone: " << _Phone /*GetPhone()*/ << endl;
        cout << "Body   : " << TextMessage << endl;
    }

    void PrintInfo()
    {
        cout << "Info:";
        cout << "\n---------------------------\n\n";
        cout << "ID             :" << _ID << endl;
        cout << "FirstName      :" << _FirstName << endl;
        cout << "LastName       :" << _LastName << endl;
        cout << "Full Name      :" << _FullName << endl;
        cout << "Email          :" << _Email << endl;
        cout << "Phone          :" << _Phone << endl;

        cout << "\n\n---------------------------\n";

    }
};

// Inheritance concepts;
class clsEmployee : public clsPerson
{
private:
    string _Title;
    float _Salary = 0;
    string _Department;

public:

    void SetTitle(string Title)
    {
        _Title = Title;
    }

    string GetTitle()
    {
        return _Title;
    }

    void SetSalary(float Salary)
    {
        _Salary = Salary;
    }

    float GetSalary()
    {
        return _Salary;
    }

    void SetDepartment(string Department)
    {
        _Department = Department;
    }

    string GetDepartment()
    {
        return _Department;
    }



};


int main()

{
    clsEmployee Employee1;

    Employee1.SetEmail("ayoub@gmail.com");
    Employee1.SetFirstName("Ayoub");
    Employee1.PrintInfo();
    Employee1.SendEmail("Bro", "How are you?");
    Employee1.SetSalary(3400);
    cout << Employee1.GetSalary();




   /* clsEmployee Employee(10, "Ayoub", "Fellah", "Ayoub Fellah", "Development", "ayoubfellah@gmail.com", "0714710674" , 4000 , "B");
    Employee.PrintInfo();

    Employee.SendEmail("Men", "How are you?");
    Employee.SendSMS("How are you?");*/
    system("pause>0");
    return 0;
}