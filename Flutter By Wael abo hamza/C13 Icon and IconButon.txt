import 'package:flutter/material.dart';

// تعريف القائمة هنا خارج الـ class
final List employee = [
  {"name": "Ayoub", "age": 28, "last Name": "Fellah"},
  {"name": "Saad", "age": 20, "last Name": "Fellah"},
  {"name": "Salim", "age": 18, "last Name": "Fellah"},
  {"name": "Houda", "age": 48, "last Name": "Fellah"},
  {"name": "Salima", "age": 15, "last Name": "Fellah"},
];

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: const Text("Ayoub fellah")),
        body: Container(
          height: 600,
          margin: EdgeInsets.all(20),
          child: Column( 
            children: [
              Icon(Icons.account_circle_outlined, color: Colors.red, size: 80),
              
              IconButton(
                onPressed: () => print("Icon Button"), 
                icon:  Icon(Icons.ac_unit_outlined , size: 20,),
                //iconSize: 20,
              ),
            ],
          ),
        ),
      ),
    );
  }
}
