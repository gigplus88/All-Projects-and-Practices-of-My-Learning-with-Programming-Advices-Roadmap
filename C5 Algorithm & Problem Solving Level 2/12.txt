#include <iostream>
#include <cmath>
#include <string>
using namespace std;

enum enIsperfectNumber { Perfect = 1, NotPerfect = 2 };
enIsperfectNumber IsperfectNumber;

int ReadPositiveNumber(string Message)
{
    int Number;

    do
    {
        cout << Message << endl;
        cin >> Number;

    } while (Number <= 0);

    return Number;
}

void GetInvertedPattern(int Number)
{
    for (int i = Number; i >=1; i--)
    {
        for (int j = i; j >= 1 ; j--)
        {
            cout << i;
        }
        cout << endl;
    }
}








int main()
{
    int Number = ReadPositiveNumber("Enter your number please ");
   
    GetInvertedPattern(Number);

 
    return 0;

}
