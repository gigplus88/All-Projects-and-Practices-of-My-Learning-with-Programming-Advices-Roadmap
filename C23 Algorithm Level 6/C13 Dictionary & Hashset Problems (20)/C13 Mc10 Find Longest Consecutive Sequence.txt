
//My Solution




using System;

using System.Collections.Generic;
using System.Linq;



class Program
{


    static void Main()
    {
        /*
        Example:
            Input: [100, 4, 200, 1, 3, 2]
            Output: 4 (sequence: [1, 2, 3, 4])


         */


        Dictionary<char,int> frequence = new Dictionary<char,int>();



        int[] Input = new int[] { 100, 4, 200, 1, 3, 2 };

        //var  Out = Input.OrderBy(x=>x).ToList();



        //foreach (var item in Out)
        //{
        //    Console.WriteLine(item);
        //}
        HashSet<int> frequence1 = new HashSet<int>(Input);

        int currentStreak = 0 , longestStreak =1;

        //foreach (var item in frequence1)
        //{
        //    int num = item;
        //    if (frequence1.Contains(num))
        //    {
        //        while (frequence1.Contains(num ))
        //        {
        //            num++;
        //            currentStreak++;
        //        }
        //    }
        //    longestStreak = Math.Max(longestStreak, currentStreak);
        //    currentStreak = 0;
        //}
        //  Console.WriteLine($"Counter is: {longestStreak}") ;





        // is more performe than the begin

        var Out = Input.OrderByDescending(x => x).ToList();
        List<int> freq = new List<int>(Out) ;

        foreach (var item in freq)
        {
            int num = item;
            if (freq.Contains(num))
            {
                while (freq.Contains(num))
                {
                    num++;
                    currentStreak++;
                }
            }
            longestStreak = Math.Max(longestStreak, currentStreak);
               currentStreak = 0;
            
        }
        Console.WriteLine($"Counter is: {longestStreak}") ;





        Console.ReadKey();
    }
}


