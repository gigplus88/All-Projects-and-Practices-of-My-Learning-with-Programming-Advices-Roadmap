#include <iostream>
#include <string>
#include <vector>
#include <iomanip>

using namespace std;

string ReadString()
{
    string  S1;
    cout << "Please Enter Your String?" << endl;
    getline(cin, S1);

    return S1;
}


string ReplaceWordsInString(string S1, string WordToReplace, string NewWord)
{
    short  pos = S1.find(WordToReplace);

    while (pos != std::string::npos)
    {
        S1 = S1.replace(pos, WordToReplace.length(), NewWord);
        pos = S1.find(WordToReplace); //   find the next

    }
    return S1;
         
}






int main()
{
    vector<string> vSplit;

   
    string S1 = " Welcome to Fes , Fes is a nice city";
    string StringToReplace = "Fes";
    string ReplaceTo = "Meknes";

    cout << "\nOriginal string:" << S1;
    cout << "\nstring after replace:";
    cout << "n" << ReplaceWordsInString( S1,  "Fes", "Meknes");


    system("pause>0");

    return 0;

}