using System;
using System.Collections;

using System.Threading;
class Program
{

    static BitArray IntToBitArray(int num)
    {

        BitArray bits = new BitArray(32);
        bool Zero = true;
        for (int i = 0; i < 32; i++)
        {
            bits[i] = (num & (1 << i)) != 0;
        }
        return bits;
    }

    static BitArray IntToBitArrayOptimized(int num)
    {
        int[] tempArray = { num };

        return new BitArray(tempArray);
    }
    static void Main()
    {




        //BitArray bits = IntToBitArray(5);
        //Console.WriteLine("Integer value of BitArray: " + string.Join("," , bits)  );



        //IntToBitArrayOptimized(5);

        //int myNumber = 5;

        //BitArray result = IntToBitArray(myNumber);

        //Console.WriteLine($"int number    {myNumber}");

        //for (int i = 0; i < 8; i++)
        //{
        //    Console.Write(result[i] ? "1" : "0");
        //}





        int number = 10; // Binary: 1010
        BitArray bits2 = IntToBitArrayOptimized(number);


        //Console.Write("BitArray representation of " + number + ": ");
        //bool leadingZero = true; // To suppress leading zeros
        //for (int i = bits2.Length - 1; i >= 0; i--)
        //{
        //    if (bits2[i])
        //    {
        //        leadingZero = false;
        //    }
        //    if (!leadingZero)
        //    {
        //        Console.Write(bits2[i] ? "1" : "0");
        //    }
        //}
        //// In case the number is 0, print a single "0"
        //if (leadingZero)
        //{
        //    Console.Write("0");
        //}


        Console.WriteLine("\nPress any key to exit...");
        bool leadingZero = true; // To suppress leading zeros


        for (int i = 0; i < bits2.Length; i++)
        {
            
            if (bits2[i]) //First true
            {
                leadingZero = false;
            }
            
            if (!leadingZero)
            {
                Console.Write(bits2[i] ? "1" : "0");

            }
        }

        Console.ReadKey();
    }
}





