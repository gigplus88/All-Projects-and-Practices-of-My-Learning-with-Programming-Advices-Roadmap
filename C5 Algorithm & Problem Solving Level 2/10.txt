#include <iostream>
#include <cmath>
#include <string>
using namespace std;

enum enIsperfectNumber { Perfect = 1, NotPerfect = 2 };
enIsperfectNumber IsperfectNumber;

int ReadPositiveNumber(string Message)
{
    int Number;

    do
    {
        cout << Message << endl;
        cin >> Number;

    } while (Number <= 0);

    return Number;
}

bool CheckPerfect(int Number)
{
     int sum = 0;
    for (int i = 1; i <= Number - 1; i++)
    {

        if (Number % i == 0)
        {
            sum = sum + i;

        }

    }
    return Number == sum;
    
}




void GetPerfectNumbersFrom1ToNumber(int Number)
{
    
    cout << endl << "Perfect numbers from 1 to " << Number << "is : " << endl;

    for (int i = 1; i <= Number; i++)
    {

        if (CheckPerfect(i))
        {
            cout << i << endl;

        }
    }

}

int ReverseNumber(int Number)
{
  
    int Remainder = 0, Number2=0, Remainder1=0;

    while (Number > 0)
    {
        Remainder = Number % 10;
        Number = Number / 10;
        Number2 = Number2 * 10 + Remainder;
    }
    return Number2;


}

int PrintDigits(int Number2)
{
    int Remainder = 0, Remainder1 = 0;
    while (Number2 > 0)
    {
        Remainder1 = Number2 % 10;
        Number2 = Number2 / 10;


        cout << Remainder1 << endl;
    }

    return Remainder1;
}


int main()
{
    int Number = ReadPositiveNumber("Enter your number please ");
    
    PrintDigits(ReverseNumber( Number));
      
 
    return 0;

}

