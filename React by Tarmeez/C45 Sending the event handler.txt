

//The component 
import "./index.css";

export default function MyComponent({Value , handleChange , inputName} ) {

    return (
        <>
            <label>{inputName}</label>
            <input
            className="inputStyle"
            type="text"
            name="phone"
            value={Value}
            onChange= {(e) => {handleChange(e.target.value)  }}
            />
        </>

    )

}









 function handleChangeCmp(key, value) {
    setFormInputs({ ...formInputs, [key]: value });
  }


<MyComponent
Value={formInputs.name}
handleChange={handleChangeCmp.bind(null, "name")}
inputName={"Name"}
  />
