#include <iostream>
#include <cmath>
#include <string>
using namespace std;

string ReadPositiveNumber()
{/*
    int Number;*/

    /*do
    {
        cout << Message << endl;
        cin >> Number;

    } while (Number <= 0);

    return Number;*/
    string Password;
    cout << "Enter your Password" << endl;
     cin >> Password;

     return Password;
}

bool GuessPassword(string Password)
{
          
    string word = "";
    int l = 1;
    for (int i = 65; i <= 90; i++)
    {

        for (int j = 65; j <= 90; j++)
        {

            for (int k = 65; k <= 90; k++)
            {
                //cout << char(i)  <<char(j)<< char(k)  << endl;
                word = word + char(i);
                word = word + char(j);
                word = word + char(k);

                

                

                cout << "Trial" << "[ " << l << " ]: " << word << endl;
                if (word == Password)

                {
                    cout << "Password is: " << word << endl;
                    cout << "Found after " << l << " Trial" << endl;
                    return true;
                }

                else
                {
                    word = "";
                    l++;
                }
                

               

               
            }
          

        }
        
        return false;

    }

        
    

    
}







int main()
{
    //int Number = ReadPositiveNumber("Enter your number please ");
    string Password=ReadPositiveNumber();
    GuessPassword(Password);

 
    return 0;

}

