

using System;

using System.Collections.Generic;
using System.Linq;
using System.Runtime.Remoting.Messaging;
using static Guna.UI2.Native.WinApi;



class Program
{

    static SortedSet<int> RemoveElementsFromRange(int[] elements , int Range )
    {
        SortedSet<int> s1 = new SortedSet<int>(elements) ;

        
         //s1.RemoveWhere( x => x>Range) ;
        return   s1.GetViewBetween(s1.Min , Range);
       // return s1;
    }

    static void Main()
    {


        /*
        Example:
        Input: set1 = [1, 2, 3], set2 = [3, 4, 5]
        Output: [1, 2, 3, 4, 5]
     */




        int[] input = { 1, 2, 4, 3 , 6 };


        

        Console.WriteLine(string.Join(", ", RemoveElementsFromRange( input , 3 ))); // Output: 1, 2, 3, 4, 5


        Console.ReadKey();


        // Teacher solution


        /*
            SortedSet<int> sortedSet = new SortedSet<int> { 1, 2, 3, 4, 5 };
        var range = sortedSet.GetViewBetween(int.MinValue, 3);

        sortedSet = new SortedSet<int>(range);
        Console.WriteLine(string.Join(", ", sortedSet)); // Output: 1, 2, 3
        Console.ReadKey();
         */
    }
}


