#include <iostream>
#include <string>
#include <iomanip>

using namespace std;

enum enWatToCount {SmallLetters =1 , CapitallLetters=2 , All=3};
string ReadString()
{
    string  S1;
    cout << "Please Enter Your String?" << endl;
    getline(cin, S1);
    
    return S1;
}

short CounterLetters(enWatToCount WatToCount , string S1)
{
    short Counter=0;
   
    if (WatToCount == enWatToCount::All)
    {
        return S1.length();
    }

    for (short i = 0; i < S1.length(); i++)
    {
        
        if (WatToCount == enWatToCount::CapitallLetters && isupper(S1[i]))
       
             Counter++;
            
        else if (WatToCount == enWatToCount::SmallLetters && islower(S1[i]))
        {
            Counter++;
        }

    }

   
      
    return Counter  ;
   
    
}


int CounterOfCapitalLetterCase( string S1)
{
    short CapitalCounter = 0 ;

    for (short i = 0; i < S1.length(); i++)
    {

        if (isupper(S1[i]))
        {
             CapitalCounter++;
        }
  
    }
    return CapitalCounter;
}

int CounterOfSmallLetterCase( string S1)
{
    short SmallCounter = 0;

    for (short i = 0; i < S1.length(); i++)
    {

        if (islower(S1[i]))
        {
             SmallCounter++;
        }

    }
    return SmallCounter;
}



int main()
{
    

    string S1 = ReadString();

    cout << "String Length = " << S1.length() << endl;
    cout << "Capital Letters Count= " << CounterOfCapitalLetterCase( S1) << endl;
    cout << "Small Letters Count= " << CounterOfSmallLetterCase( S1 )<< endl;

    // Solution 2
    cout << "String Length = " << CounterLetters(enWatToCount::All,S1 ) << endl;
    cout << "Capital Letters Count= " << CounterLetters( enWatToCount::CapitallLetters,  S1) << endl;
    cout << "Small Letters Count= " << CounterLetters(enWatToCount::SmallLetters, S1) << endl;


    system("pause>0");

    return 0;
}
