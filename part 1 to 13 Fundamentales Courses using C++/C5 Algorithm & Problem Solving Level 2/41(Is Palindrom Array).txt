#include <iostream>
#include <cmath>
#include <string>
#include <cstdlib>

using namespace std;



int ReadPositiveNumber(string Message)
{

    int Number;

    do
    {
        cout << Message << endl;
        cin >> Number;

    } while (Number <= 0);

    return Number;
}

void FillArray(int arr[6], int& arrLength)

{
    arrLength = 6;
    arr[0] = 10;
    arr[1] = 20;
    arr[2] = 30;
    arr[3] = 30;
    arr[4] = 20;
    arr[5] = 10;
   

}


void PrintArray(int arr[6], int &arrLength)

{
    arrLength = 6;
    cout << arr[0] << " ";
    cout << arr[1] << " ";
    cout << arr[2] << " ";
    cout << arr[3] << " ";
    cout << arr[4] << " ";
    cout << arr[5] << " ";
    
}


bool IsPalindromeArray(int arr[6], int &arrLength )
{
    
    for ( int i = 0; i < arrLength ; i++)
    {
        if (arr[i] != arr[arrLength - i - 1])
        {
            return 0;
        }


    }

    return 1;

}
















int main()
{
    srand((unsigned)time(NULL));

    int arr2[6] ,arr[6] , arrLength = 0;

    FillArray(arr, arrLength);

    cout << "Array 1 elements: " << endl;
    PrintArray(arr,  arrLength);


     cout << endl;


    if (IsPalindromeArray( arr,  arrLength)==1)
    {
        cout << "Yes array is Plindrom" << endl;

    }

    else cout << "No array is  not Plindrom" << endl;





    return 0;

}

